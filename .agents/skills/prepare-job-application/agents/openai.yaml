interface:
  display_name: "Prepare Job Application"
  short_description: "Prepare answers with evidence provenance"
  default_prompt: "Use $prepare-job-application to prepare a role-specific packet with best-supported answers and compensation policy."
