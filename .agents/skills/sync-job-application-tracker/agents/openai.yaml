interface:
  display_name: "Sync Application Tracker"
  short_description: "Sync application lifecycle rows idempotently"
  default_prompt: "Use $sync-job-application-tracker to reconcile the local ledger with the Google Sheet."
